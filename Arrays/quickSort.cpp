#include<bits/stdc++.h>
using namespace std;
void quickSort(vector<int>arr){
    int low=0,high=arr.size()-1;
    for()
}
int main(){
    int N;
    vector<int>arr;
    for(int i=0;i<N;i++){
        int item;
        cin>>item;
        arr.push_back(item);
    }
    quickSort(arr);
}